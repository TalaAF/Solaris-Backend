spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lmsdb
    username: postgres
    password: A!ciRche@postG
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update  # Keep this as "update" to preserve schema
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  # Remove the H2 console configuration as it's not needed for PostgreSQL

server:
  port: 8080

logging:
  level:
    org.springframework: INFO
    com.example.lms: DEBUG
    org.hibernate.SQL: DEBUG
    io.jsonwebtoken: DEBUG

# JWT Configuration
jwt:
  expiration: 86400000  # 24 hours in milliseconds