spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lmsdb
    username: postgres
    password: A!ciRche@postG  # Replace with your actual PostgreSQL password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

logging:
  level:
    org.springframework: INFO
    com.example.lms: DEBUG
    org.hibernate.SQL: DEBUG
    io.jsonwebtoken: DEBUG

# JWT Configuration
jwt:
  expiration: 86400000  # 24 hours in milliseconds