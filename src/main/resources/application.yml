spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/my_database
    username: postgres
    password: MST
  jpa:
    hibernate:
      ddl-auto: update  
    show-sql: true  
    properties:
      hibernate:
        format_sql: true